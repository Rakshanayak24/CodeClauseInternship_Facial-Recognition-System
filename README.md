# OpenCV Face Detection System

## How to Run
1. Install dependencies:
```
pip install -r requirements.txt
```

2. Run the script:
```
python main.py
```

3. Enter:
```
input_sample.jpg
```

This system uses OpenCV Haarcascade (works on all Windows systems). No CMake or dlib required.
